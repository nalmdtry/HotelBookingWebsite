1. Tải dự án về máy.
2. Chạy lệnh `composer install` trong cửa sổ terminal để cài đặt thư viện.
3. Import database từ thư mục `database/hbwebsite.sql`.
4. Cấu hình thông tin kết nối trong `admin/inc/db.config.php`.
5. Mở XAMPP và truy cập dự án qua trình duyệt.
